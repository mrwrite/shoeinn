# Tasks

## 1. OpenSpec artifacts

- [x] Create proposal, design, spec, and tasks for the docs-only change.

## 2. Root documentation

- [x] Update `README.md` with Windows PowerShell startup instructions for Postgres, `apps/api`, and `apps/mobile`.
- [x] Clarify that the repo should not be run as a single root-level app.
- [x] Document `EXPO_PUBLIC_API_URL` handling for Android emulator, iOS simulator, and physical devices.
- [x] Add a concise local validation checklist for provider claiming and assignment.

## 3. Backend documentation

- [x] Update `apps/api/README.md` to fix venv activation, dependency install path, `.env` host guidance, migrations, API startup, and focused test commands.

## 4. Payment service documentation

- [x] Add the minimum documentation needed for `apps/payment`, explicitly stating that it is optional for most local development and underdocumented today.

## 5. Verification

- [x] Verify the OpenSpec artifacts and updated docs are present and internally consistent.
